const React = require('react');

const ChosenCards = React.createClass({

  render: function(){ 

    return (
      <div id="chosen-cards">
        <h4>Chosen Cards</h4>
      </div>
      )

  }

})

module.exports = ChosenCards;